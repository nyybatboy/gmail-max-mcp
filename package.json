{
  "name": "gmail-mcp",
  "version": "0.3.0",
  "private": true,
  "description": "Mike's max-unlock Gmail CLI + MCP server. Full Gmail API surface: send, drafts with attachments, threaded replies, labels, filters, settings (vacation/signatures/forwarding/delegates/IMAP/POP), watch, history. Auth: OAuth 2.0 desktop flow with Mike's own Cloud Console OAuth client (gcloud's ADC client cannot grant Gmail's restricted scopes).",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "gmail-cli": "bin/gmail-cli.js",
    "gmail-mcp": "bin/gmail-mcp.js"
  },
  "scripts": {
    "auth": "node bin/gmail-cli.js auth",
    "auth-status": "node bin/gmail-cli.js auth-status",
    "mcp": "node bin/gmail-mcp.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "googleapis": "^144.0.0",
    "open": "^10.1.0"
  }
}
